<location> .config/wal/templates/dunstrc

[global]
frame_color = "{color2}"
separator_color= frame
scale = <dpi_factor>
icon_corner_radius = 5
font = "Opensans 12"
icon_theme = "Papirus-Dark"
origin = top-center
offset = 0
frame_width = 4
corners = all
corner_radius = 10
dmenu = /usr/bin/rofi -dmenu -p dunst:

[urgency_low]
background = "{background}"
foreground = "{foreground}"

[urgency_normal]
background = "{background}"
foreground = "{foreground}"

[urgency_critical]
background = "{background}"
foreground = "{foreground}"
frame_color = "{color10}"

